version: 1.1
runtime: php81
build:
  commands:
    pre-build:
      - echo In pre build stage
    build:
      - ./01_setup_environment.sh
    post-build:
      - ./01_composer_install.sh
run:
  runtime-version: 8.1
  command: ./start.sh
  network:
    port: 8000