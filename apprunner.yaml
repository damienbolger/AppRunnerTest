version: 1.0
runtime: php81
build:
  commands:
    build:
      - echo Now in Build
    post-build:
      - echo Now in Post Build
  env:
    - name: GRAPHQL_DEV_MODE
      value: true
run:
  runtime-version: 8.1
  command: echo Now in Run
  network:
    port: 8000
    env: MY_APP_PORT
  env:
    - name: MY_VAR_EXAMPLE
      value: "example"
